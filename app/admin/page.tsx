'use client';

import { ChangeEvent, useEffect, useMemo, useState } from 'react';
import Link from 'next/link';

type C = { id: number; catId: number; name: string; nameEn: string; vpn: boolean; iran: boolean; category: string; platform?: string | null; satellite?: string | null; frequency?: string | null; sources: { id: number | null }[] };
type PreviewRow = { row: number; type: string; name?: string; nameEn?: string; url?: string; group?: string; category?: string; country?: string; satellite?: string; frequency?: string; extra?: string };
type UploadPhase = 'reading' | 'analyzing' | 'uploading' | 'processing' | 'done' | 'error';
type Upload = { id: string; file: File; status: 'sending' | 'done' | 'error'; phase: UploadPhase; readProgress: number; analysisProgress: number; uploadProgress: number; startedAt: number; elapsedMs: number; etaMs: number | null; preview: PreviewRow[]; totalRows: number; message?: string; result?: { detectedTable?: string; confidence?: number; format?: string; rows: number; created: number; updated: number; skipped: number; errors: string[]; ingestion?: { channelsCreated?: number; channelsMatched?: number; sourcesCreated?: number; sourcesSkipped?: number }; database?: { categories?: number; channels?: number; sources?: number; programs?: number } } };
const labels: Record<string, string> = { category: 'Category', channel: 'Channel', source: 'Source', program: 'Program' };
const formats = '.csv,.json,.m3u,.m3u8';
const sleep = (ms: number) => new Promise<void>(resolve => setTimeout(resolve, ms));

function csvPreview(text: string): Array<Record<string, string>> { const input = text.replace(/^\uFEFF/, ''); const rows: string[][] = []; let row: string[] = [], cell = '', quoted = false; for (let i = 0; i < input.length; i += 1) { const ch = input[i]; if (ch === '"') { if (quoted && input[i + 1] === '"') { cell += '"'; i += 1; } else quoted = !quoted; } else if (ch === ',' && !quoted) { row.push(cell); cell = ''; } else if ((ch === '\n' || ch === '\r') && !quoted) { if (ch === '\r' && input[i + 1] === '\n') i += 1; row.push(cell); cell = ''; if (row.some(v => v.trim() !== '')) rows.push(row); row = []; } else cell += ch; } if (cell || row.length) { row.push(cell); if (row.some(v => v.trim() !== '')) rows.push(row); } if (!rows.length) return []; const headers = rows.shift()!.map((h, i) => h.trim() || `column_${i + 1}`); return rows.map(values => Object.fromEntries(headers.map((h, i) => [h, (values[i] ?? '').trim()]))); }
function attrValue(header: string, key: string) { const re = new RegExp(`${key}="([^"]*)"`, 'i'); return header.match(re)?.[1]?.trim() || ''; }
function m3uPreview(text: string): Array<Record<string, string>> { const lines = text.replace(/^\uFEFF/, '').split(/\r?\n/); const entries: Array<Record<string, string>> = []; let pending: Record<string, string> | null = null; let pendingRef = ''; let pendingOrigin = ''; for (const raw of lines) { const line = raw.trim(); if (!line || line === '#EXTM3U') continue; if (line.startsWith('#EXTINF:')) { const comma = line.indexOf(','); const header = comma >= 0 ? line.slice(0, comma) : line; const displayName = comma >= 0 ? line.slice(comma + 1).trim() : ''; pending = { name: attrValue(header, 'tvg-name') || displayName || attrValue(header, 'tvg-id') || 'Unnamed channel', nameEn: attrValue(header, 'tvg-name') || attrValue(header, 'tvg-id') || displayName, group: attrValue(header, 'group-title'), country: attrValue(header, 'tvg-country') || attrValue(header, 'country'), satellite: attrValue(header, 'satellite') || attrValue(header, 'sat'), frequency: attrValue(header, 'frequency') || attrValue(header, 'freq'), logo: attrValue(header, 'tvg-logo') || attrValue(header, 'logo') }; pendingRef = ''; pendingOrigin = ''; continue; } if (line.startsWith('#EXTVLCOPT:') && pending) { const directive = line.slice('#EXTVLCOPT:'.length); const separator = directive.indexOf('='); const key = (separator >= 0 ? directive.slice(0, separator) : directive).trim().toLowerCase(); const value = separator >= 0 ? directive.slice(separator + 1).trim() : ''; if (key.includes('referrer') || key.includes('referer')) pendingRef = value; if (key === 'http-origin') pendingOrigin = value; continue; } if (line.startsWith('#EXTHTTP:') && pending) { try { const payload = JSON.parse(line.slice('#EXTHTTP:'.length).trim()) as Record<string, unknown>; pendingRef = String(payload.Referer ?? payload.referer ?? payload['http-referrer'] ?? payload['http-referer'] ?? pendingRef).trim(); pendingOrigin = String(payload.Origin ?? payload.origin ?? pendingOrigin).trim(); } catch {} continue; } if (line.startsWith('#') || !pending || !/^https?:\/\//i.test(line)) continue; entries.push({ ...pending, url: line, referer: pendingRef, origin: pendingOrigin }); pending = null; } return entries; }
function jsonPreview(text: string): Array<Record<string, unknown>> { const payload = JSON.parse(text.replace(/^\uFEFF/, '')) as unknown; if (Array.isArray(payload)) return payload.filter(v => Boolean(v && typeof v === 'object')) as Array<Record<string, unknown>>; if (payload && typeof payload === 'object') { const value = payload as Record<string, unknown>; for (const key of ['rows', 'data', 'channels', 'sources', 'programs', 'categories', 'posts']) if (Array.isArray(value[key])) return value[key].filter(v => Boolean(v && typeof v === 'object')) as Array<Record<string, unknown>>; } return []; }
function buildPreview(fileName: string, text: string): Array<Record<string, unknown>> { const lower = fileName.toLowerCase(); if (lower.endsWith('.m3u') || lower.endsWith('.m3u8') || text.trimStart().startsWith('#EXTM3U')) return m3uPreview(text); if (lower.endsWith('.json') || text.trimStart().startsWith('{') || text.trimStart().startsWith('[')) return jsonPreview(text); return csvPreview(text); }
function rowToPreview(row: Record<string, unknown>, index: number, format: string): PreviewRow { const pick = (...keys: string[]) => { for (const key of keys) { const hit = Object.keys(row).find(k => k.trim().toLowerCase().replace(/[\s-]+/g, '_') === key); if (hit) return String(row[hit] ?? ''); } return ''; }; const url = pick('url', 'channel_url', 'source_url', 'stream_url'); const name = pick('name', 'channel_name', 'channelname', 'title', 'program_title'); const nameEn = pick('name_en', 'channel_name_en', 'nameen'); const group = pick('group-title', 'group_title', 'category', 'category_name'); return { row: index + 1, type: format === 'm3u' ? 'Channel + Source' : 'Detected row', name, nameEn, url, group, category: pick('category', 'category_name'), country: pick('country', 'tvg-country'), satellite: pick('satellite', 'sat'), frequency: pick('frequency', 'freq'), extra: format === 'json' ? JSON.stringify(row).slice(0, 240) : '' }; }
function xhrUpload(url: string, form: FormData, token: string, onProgress: (value: number) => void) { return new Promise<{ status: number; body: any }>((resolve, reject) => { const xhr = new XMLHttpRequest(); xhr.open('POST', url); if (token) xhr.setRequestHeader('x-admin-token', token); xhr.upload.onprogress = e => { if (e.lengthComputable) onProgress(Math.round((e.loaded / e.total) * 100)); }; xhr.onload = () => { try { resolve({ status: xhr.status, body: JSON.parse(xhr.responseText || '{}') }); } catch { reject(new Error('Invalid server response')); } }; xhr.onerror = () => reject(new Error('Network error')); xhr.onabort = () => reject(new Error('Upload aborted')); xhr.send(form); }); }
function etaFromProgress(startedAt: number, progress: number) { if (progress <= 0) return null; const elapsed = Date.now() - startedAt; return Math.max(0, Math.round((elapsed / progress) * (100 - progress))); }
function formatEta(ms: number | null) { if (ms == null || !Number.isFinite(ms)) return 'محاسبه می‌شود…'; const seconds = Math.ceil(ms / 1000); if (seconds < 60) return `${seconds} ثانیه`; return `${Math.floor(seconds / 60)} دقیقه و ${seconds % 60} ثانیه`; }
function csvEscape(value: unknown) { const text = String(value ?? ''); return /[",\r\n]/.test(text) ? `"${text.replace(/"/g, '""')}"` : text; }
function serializeCsv(headers: string[], rows: Array<Record<string, unknown>>) { return `${headers.map(csvEscape).join(',')}\n${rows.map(row => headers.map(header => csvEscape(row[header])).join(',')).join('\n')}\n`; }

export default function Admin() {
  const [data, setData] = useState<C[]>([]); const [q, setQ] = useState(''); const [cat, setCat] = useState(''); const [msg, setMsg] = useState('در حال دریافت کاتالوگ…'); const [uploads, setUploads] = useState<Upload[]>([]); const [sending, setSending] = useState(false); const [token, setToken] = useState('');
  const load = () => fetch('/api/catalog').then(r => r.json()).then(j => { setData(j.channels || []); setMsg('آماده'); }).catch(() => setMsg('خطا در دریافت کاتالوگ'));
  useEffect(() => { setToken(window.localStorage.getItem('momsat-admin-token') || ''); load(); }, []);
  const cats = Array.from(new Map(data.map(c => [c.catId, c.category]))); const filtered = useMemo(() => data.filter(c => (!q || `${c.name} ${c.nameEn}`.toLowerCase().includes(q.toLowerCase())) && (!cat || String(c.catId) === cat)), [data, q, cat]); const sources = data.reduce((n, c) => n + c.sources.length, 0);
  const patchUpload = (id: string, patch: Partial<Upload>) => setUploads(u => u.map(x => x.id === id ? { ...x, ...patch } : x));

  async function sendUpload(item: Upload) {
    setSending(true); const startedAt = item.startedAt; patchUpload(item.id, { status: 'sending', phase: 'reading', message: undefined }); setMsg(`در حال خواندن ${item.file.name}…`);
    try {
      const text = await new Promise<string>((resolve, reject) => { const reader = new FileReader(); reader.onprogress = e => { const progress = e.lengthComputable ? Math.round((e.loaded / e.total) * 100) : 0; patchUpload(item.id, { readProgress: progress, elapsedMs: Date.now() - startedAt, etaMs: etaFromProgress(startedAt, progress) }); }; reader.onload = () => { patchUpload(item.id, { readProgress: 100, elapsedMs: Date.now() - startedAt }); resolve(String(reader.result || '')); }; reader.onerror = () => reject(new Error('خواندن فایل ناموفق بود')); reader.readAsText(item.file); });
      patchUpload(item.id, { phase: 'analyzing', message: 'ساخت preview و تحلیل رکوردها…' }); setMsg(`در حال تحلیل رکوردهای ${item.file.name}…`);
      const rawRows = buildPreview(item.file.name, text); const format = /\.m3u8?$/i.test(item.file.name) || text.trimStart().startsWith('#EXTM3U') ? 'm3u' : /\.json$/i.test(item.file.name) || /^[\[{]/.test(text.trimStart()) ? 'json' : 'csv'; const previewLimit = 200; const previewRows: PreviewRow[] = [];
      for (let i = 0; i < rawRows.length; i += 1) { if (i < previewLimit) previewRows.push(rowToPreview(rawRows[i], i, format)); if (i % 250 === 0 || i === rawRows.length - 1) { const progress = rawRows.length ? Math.round(((i + 1) / rawRows.length) * 100) : 100; patchUpload(item.id, { analysisProgress: progress, totalRows: rawRows.length, preview: [...previewRows], elapsedMs: Date.now() - startedAt, etaMs: etaFromProgress(startedAt, Math.max(1, progress)) }); await sleep(0); } }
      patchUpload(item.id, { analysisProgress: 100, totalRows: rawRows.length, preview: previewRows, phase: 'uploading', elapsedMs: Date.now() - startedAt, etaMs: null }); setMsg(`تحلیل تمام شد؛ در حال ارسال ${rawRows.length.toLocaleString()} رکورد…`);

      if (format === 'csv' && rawRows.length > 0) {
        const headers = Object.keys(rawRows[0]); const chunkSize = 50; const aggregate: NonNullable<Upload['result']> = { format: 'csv', rows: 0, created: 0, updated: 0, skipped: 0, errors: [], ingestion: { channelsCreated: 0, channelsMatched: 0, sourcesCreated: 0, sourcesSkipped: 0 }, database: undefined };
        for (let offset = 0; offset < rawRows.length; offset += chunkSize) {
          const chunkRows = rawRows.slice(offset, offset + chunkSize); const chunkText = serializeCsv(headers, chunkRows); const form = new FormData(); form.append('file', new File([chunkText], `${item.file.name}.chunk-${Math.floor(offset / chunkSize) + 1}.csv`, { type: 'text/csv' }));
          const chunkResponse = await xhrUpload('/api/admin/table-import/chunk', form, token, () => {}); const j = chunkResponse.body || {};
          if (!aggregate.detectedTable) { aggregate.detectedTable = j.detectedTable; aggregate.confidence = j.confidence; }
          aggregate.rows += Number(j.rows || chunkRows.length); aggregate.created += Number(j.created || 0); aggregate.updated += Number(j.updated || 0); aggregate.skipped += Number(j.skipped || 0); aggregate.errors?.push(...(Array.isArray(j.errors) ? j.errors : j.error ? [j.error] : []));
          if (j.ingestion) { aggregate.ingestion!.channelsCreated = (aggregate.ingestion!.channelsCreated || 0) + Number(j.ingestion.channelsCreated || 0); aggregate.ingestion!.channelsMatched = (aggregate.ingestion!.channelsMatched || 0) + Number(j.ingestion.channelsMatched || 0); aggregate.ingestion!.sourcesCreated = (aggregate.ingestion!.sourcesCreated || 0) + Number(j.ingestion.sourcesCreated || 0); aggregate.ingestion!.sourcesSkipped = (aggregate.ingestion!.sourcesSkipped || 0) + Number(j.ingestion.sourcesSkipped || 0); }
          if (j.database) aggregate.database = j.database;
          const processed = Math.min(rawRows.length, offset + chunkRows.length); const progress = Math.round((processed / rawRows.length) * 100); const eta = etaFromProgress(startedAt, Math.max(1, 50 + progress / 2));
          patchUpload(item.id, { phase: 'processing', uploadProgress: progress, elapsedMs: Date.now() - startedAt, etaMs: eta, message: `ثبت رکوردها در دیتابیس: ${processed.toLocaleString()} / ${rawRows.length.toLocaleString()}` });
          setMsg(`ثبت رکوردها: ${processed.toLocaleString()} / ${rawRows.length.toLocaleString()}`); await sleep(0);
          if (chunkResponse.status >= 400) throw new Error(j.error || `Chunk ${Math.floor(offset / chunkSize) + 1} failed`);
        }
        const result = aggregate; const ok = (aggregate.errors?.length ?? 0) === 0; patchUpload(item.id, { status: ok ? 'done' : 'error', phase: ok ? 'done' : 'error', uploadProgress: 100, elapsedMs: Date.now() - startedAt, etaMs: 0, message: ok ? `ثبت ${aggregate.rows.toLocaleString()} رکورد کامل شد` : `پردازش با ${aggregate.errors?.length ?? 0} خطا تمام شد`, result }); setMsg(ok ? `پردازش ${labels[aggregate.detectedTable || ''] || aggregate.detectedTable || 'CSV'} انجام شد` : 'پردازش CSV با خطا انجام شد'); if (ok) await load(); return;
      }

      const form = new FormData(); form.append('file', item.file); const response = await xhrUpload('/api/admin/table-import', form, token, progress => { if (progress >= 100) { patchUpload(item.id, { phase: 'processing', uploadProgress: 100, elapsedMs: Date.now() - startedAt, etaMs: null, message: 'آپلود فایل کامل شد؛ سرور در حال ثبت رکوردها در دیتابیس است…' }); setMsg('آپلود کامل شد؛ در حال ثبت رکوردها در دیتابیس…'); return; } patchUpload(item.id, { phase: 'uploading', uploadProgress: progress, elapsedMs: Date.now() - startedAt, etaMs: etaFromProgress(startedAt, progress) }); });
      patchUpload(item.id, { phase: 'processing', uploadProgress: 100, elapsedMs: Date.now() - startedAt, etaMs: null, message: 'فایل به سرور رسید؛ در حال پردازش و ثبت در دیتابیس…' }); setMsg('فایل به سرور رسید؛ در حال ثبت رکوردها در دیتابیس…'); const j = response.body || {}; const ok = response.status >= 200 && response.status < 300;
      patchUpload(item.id, { status: ok ? 'done' : 'error', phase: ok ? 'done' : 'error', elapsedMs: Date.now() - startedAt, etaMs: 0, message: j.error || `${j.format === 'm3u' ? 'M3U Playlist' : (labels[j.detectedTable] || j.detectedTable || 'Data')} | ${j.rows || 0} entries | +${j.created || 0} records | ${j.updated || 0} updated | ${j.skipped || 0} skipped`, result: j }); setMsg(j.error || (ok ? `پردازش ${j.format === 'm3u' ? 'M3U' : (labels[j.detectedTable] || j.detectedTable)} انجام شد` : 'خطا در import')); if (ok) await load();
    } catch (error) { patchUpload(item.id, { status: 'error', phase: 'error', elapsedMs: Date.now() - startedAt, etaMs: 0, message: error instanceof Error ? error.message : 'خطای شبکه' }); setMsg('خطا در خواندن، تحلیل یا ارسال فایل'); } finally { setSending(false); }
  }
  function addFile(e: ChangeEvent<HTMLInputElement>) { const file = e.target.files?.[0]; e.target.value = ''; if (!file || sending) return; const item: Upload = { id: `${Date.now()}-${file.name}`, file, status: 'sending', phase: 'reading', readProgress: 0, analysisProgress: 0, uploadProgress: 0, startedAt: Date.now(), elapsedMs: 0, etaMs: null, preview: [], totalRows: 0 }; setUploads(u => [item, ...u]); void sendUpload(item); }
  function removeUpload(id: string) { setUploads(u => u.filter(x => x.id !== id)); }
  async function sync() { setMsg('در حال واکشی و sync کاتالوگ…'); try { const r = await fetch('/api/admin/sync', { method: 'POST', headers: token ? { 'x-admin-token': token } : undefined }); const j = await r.json(); setMsg(j.message || j.error || 'تمام شد'); await load(); } catch { setMsg('خطای شبکه'); } }
  async function syncEpg() { setMsg('در حال sync راهنمای برنامه‌ها…'); try { const r = await fetch('/api/admin/epg', { method: 'POST', headers: token ? { 'x-admin-token': token } : undefined }); const j = await r.json(); setMsg(j.status === 'unconfigured' ? 'EPG تنظیم نشده است' : j.error || `EPG: ${j.programs || 0} برنامه، ${j.matchedChannels || 0} شبکه match شد`); } catch { setMsg('خطای شبکه در EPG'); } }
  function saveToken(value: string) { setToken(value); window.localStorage.setItem('momsat-admin-token', value); }
  return <main>
    <header className="top"><div><div className="brand">MOM<span>SAT</span></div><div className="muted">Control Center</div></div><nav><Link href="/">نمایش</Link><Link href="/browse">کاتالوگ</Link><Link href="/guide">EPG / راهنما</Link></nav></header>
    <div className="eyebrow">ADMIN / UNIVERSAL INGESTION</div><h1>مدیریت کاتالوگ و دیتابیس</h1><div className="notice">یک uploader برای همه دیتاها. CSV، JSON یا M3U را انتخاب کن؛ سیستم نوع دیتا را خودش تشخیص می‌دهد، شبکه‌ها را با تطبیق محافظه‌کارانه dedupe می‌کند و Channel / Source را مستقیم وارد یا بروزرسانی می‌کند.</div>
    <div className="admin-kpis"><div className="kpi"><span className="muted">Channels</span><strong>{data.length}</strong></div><div className="kpi"><span className="muted">Sources</span><strong>{sources}</strong></div><div className="kpi"><span className="muted">Categories</span><strong>{cats.length}</strong></div><div className="kpi"><span className="muted">Uploads</span><strong>{uploads.length}</strong></div></div>
    <section style={{ marginTop: 22 }}><div className="eyebrow">UNIVERSAL DATA UPLOAD</div><h2 style={{ margin: '6px 0 4px' }}>آپلود خودکار</h2><p className="muted" style={{ marginTop: 0 }}>CSV / JSON / M3U / M3U8. بدون انتخاب دستی جدول.</p><div className="notice" style={{ marginTop: 14, padding: 24, textAlign: 'center', borderStyle: 'dashed' }}><div style={{ fontSize: 18, fontWeight: 800 }}>فایل داده یا playlist را انتخاب کن</div><div className="muted" style={{ marginTop: 7 }}>برای CSV، رکوردها به قطعات ۵۰تایی ارسال و درصد ثبت واقعی در دیتابیس نمایش داده می‌شود.</div><label className="btn primary" style={{ display: 'inline-block', marginTop: 18, cursor: sending ? 'not-allowed' : 'pointer', opacity: sending ? 0.65 : 1 }}>انتخاب فایل<input hidden disabled={sending} type="file" accept={`${formats},audio/x-mpegurl,application/vnd.apple.mpegurl`} onChange={addFile} /></label></div></section>
    <section style={{ marginTop: 20 }}><div style={{ display: 'flex', gap: 10, flexWrap: 'wrap', alignItems: 'center' }}><span className="eyebrow">ADMIN TOKEN</span><input className="status" style={{ minWidth: 280 }} type="password" value={token} onChange={e => saveToken(e.target.value)} placeholder="ADMIN_TOKEN" /></div></section>
    <section style={{ marginTop: 24 }}><div className="eyebrow">IMPORT HISTORY</div><div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', gap: 12, flexWrap: 'wrap' }}><h2 style={{ margin: '6px 0' }}>پردازش زنده فایل</h2><span className="status">{msg}</span></div>
      {uploads.length === 0 ? <div className="notice" style={{ marginTop: 12, textAlign: 'center', padding: 30 }}><strong>هنوز فایلی پردازش نشده است</strong></div> : <div style={{ display: 'grid', gap: 14, marginTop: 12 }}>{uploads.map(u => { const overall = u.phase === 'reading' ? Math.round(u.readProgress * 0.25) : u.phase === 'analyzing' ? 25 + Math.round(u.analysisProgress * 0.25) : u.phase === 'uploading' ? 50 + Math.round(u.uploadProgress * 0.35) : u.phase === 'processing' ? 50 + Math.round(u.uploadProgress * 0.5) : u.phase === 'done' ? 100 : 0; const currentProgress = u.phase === 'reading' ? u.readProgress : u.phase === 'analyzing' ? u.analysisProgress : u.phase === 'uploading' ? u.uploadProgress : u.phase === 'processing' ? u.uploadProgress : null; const phaseLabel = u.phase === 'reading' ? 'خواندن فایل' : u.phase === 'analyzing' ? 'تحلیل رکوردها' : u.phase === 'uploading' ? 'آپلود به سرور' : u.phase === 'processing' ? 'ثبت در دیتابیس' : u.phase === 'done' ? 'تکمیل شد' : 'خطا'; return <div key={u.id} className="notice" style={{ padding: 18 }}>
        <div style={{ display: 'flex', justifyContent: 'space-between', gap: 12, flexWrap: 'wrap', alignItems: 'center' }}><div><strong>{u.file.name}</strong><span className="muted" style={{ marginInlineStart: 8 }}>{(u.file.size / 1024).toFixed(1)} KB</span></div><span className="status">{phaseLabel} · {overall}%</span></div>
        <div style={{ height: 10, borderRadius: 999, background: 'rgba(255,255,255,.08)', overflow: 'hidden', marginTop: 12 }}><div style={{ width: `${overall}%`, height: '100%', transition: 'width .2s ease', background: 'var(--accent, #3b82f6)' }} /></div>
        <div style={{ display: 'flex', gap: 14, flexWrap: 'wrap', marginTop: 10, fontSize: 13 }}><span>مرحله: {currentProgress == null ? (u.phase === 'done' ? '100%' : 'در حال ثبت…') : `${currentProgress}%`}</span><span>رکوردها: {u.totalRows.toLocaleString()}</span><span>زمان: {(u.elapsedMs / 1000).toFixed(1)}s</span><span>زمان باقی‌مانده: {formatEta(u.etaMs)}</span></div>
        {u.preview.length > 0 && <div style={{ marginTop: 14 }}><div className="eyebrow">RECORD PREVIEW / FIRST {u.preview.length}</div><div style={{ overflowX: 'auto', marginTop: 8, maxHeight: 420, overflowY: 'auto' }}><table className="admin-table"><thead><tr><th>#</th><th>Type</th><th>Name</th><th>Name EN</th><th>URL</th><th>Group</th><th>Country</th><th>Satellite</th><th>Frequency</th></tr></thead><tbody>{u.preview.map(row => <tr key={`${u.id}-${row.row}`}><td>{row.row}</td><td>{row.type}</td><td>{row.name || '—'}</td><td>{row.nameEn || '—'}</td><td style={{ maxWidth: 420, wordBreak: 'break-all' }}>{row.url || row.extra || '—'}</td><td>{row.group || row.category || '—'}</td><td>{row.country || '—'}</td><td>{row.satellite || '—'}</td><td>{row.frequency || '—'}</td></tr>)}</tbody></table></div></div>}
        {u.message && <div className="status" style={{ marginTop: 12 }}>{u.message}</div>}
        {u.result && <div className="notice" style={{ marginTop: 12, padding: 14 }}><div className="eyebrow">DATABASE RESULT</div><div style={{ display: 'flex', gap: 16, flexWrap: 'wrap', marginTop: 8 }}><span>Format: {u.result.format || '—'}</span><span>Table: {labels[u.result.detectedTable || ''] || u.result.detectedTable || '—'}</span><span>Confidence: {u.result.confidence != null ? `${Math.round(u.result.confidence * 100)}%` : '—'}</span><span>Rows: {u.result.rows ?? 0}</span><span>Created: {u.result.created ?? 0}</span><span>Updated: {u.result.updated ?? 0}</span><span>Skipped: {u.result.skipped ?? 0}</span><span>Errors: {u.result.errors?.length ?? 0}</span></div>{u.result.database && <div className="muted" style={{ marginTop: 8 }}>DB totals — Categories: {u.result.database.categories ?? 0} · Channels: {u.result.database.channels ?? 0} · Sources: {u.result.database.sources ?? 0} · Programs: {u.result.database.programs ?? 0}</div>}{u.result.ingestion && <div className="muted" style={{ marginTop: 6 }}>Ingestion — Created channels: {u.result.ingestion.channelsCreated ?? 0} · Matched: {u.result.ingestion.channelsMatched ?? 0} · Sources created: {u.result.ingestion.sourcesCreated ?? 0} · Source duplicates: {u.result.ingestion.sourcesSkipped ?? 0}</div>}{u.result.errors?.length ? <pre style={{ whiteSpace: 'pre-wrap', marginTop: 10, fontSize: 12 }}>{u.result.errors.slice(0, 20).join('\n')}</pre> : null}</div>}
        <button className="btn" style={{ marginTop: 12 }} onClick={() => removeUpload(u.id)}>حذف از تاریخچه</button>
      </div>; })}</div>}
    </section>
    <section style={{ marginTop: 24, display: 'flex', gap: 10, flexWrap: 'wrap' }}><button className="btn" onClick={sync}>Sync منابع</button><button className="btn" onClick={syncEpg}>Sync EPG</button></section>
    <section style={{ marginTop: 28 }}><div className="eyebrow">CHANNEL CATALOG</div><div style={{ display: 'flex', gap: 10, flexWrap: 'wrap', alignItems: 'center' }}><input className="status" placeholder="جستجو…" value={q} onChange={e => setQ(e.target.value)} /><select className="status" value={cat} onChange={e => setCat(e.target.value)}><option value="">همهٔ دسته‌ها</option>{cats.map(([id, name]) => <option key={id} value={String(id)}>{name}</option>)}</select></div><div style={{ overflowX: 'auto', marginTop: 12 }}><table className="admin-table"><thead><tr><th>Channel</th><th>Category</th><th>VPN</th><th>Iran</th><th>Platform</th><th>Satellite</th><th>Frequency</th><th>Sources</th></tr></thead><tbody>{filtered.map(c => <tr key={c.id}><td>{c.name}<br /><span className="muted">{c.nameEn}</span></td><td>{c.category}</td><td>{c.vpn ? 'YES' : 'NO'}</td><td>{c.iran ? 'YES' : 'NO'}</td><td>{c.platform || '—'}</td><td>{c.satellite || '—'}</td><td>{c.frequency || '—'}</td><td>{c.sources.length}</td></tr>)}</tbody></table></div></section>
  </main>;
}